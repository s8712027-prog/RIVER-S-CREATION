# ANN/A current-line handoff — local ordinal-moment synthetic pass v8

## Authority

This file supersedes
`A_CURRENT_LINE_HANDOFF_ANCHOR_TRAJECTORY_FAIL_V7.md` as the continuation
authority.  V7's completed v1.43 result and all historical prohibitions remain
binding except that the user has now authorized, and the synthetic gate has
approved, one isolated branch-level hypothesis.

## Active model state

The active mainline remains unchanged and provisional.  The local-capability
reader is still the provisional default.  Its SHA-256 was rechecked as:

`74AAA1AF2AACAD0FF8B13DF5DD9D472CBE91F95D07C87F9F773352FF0DD81ACA`.

No ordinal-moment code is active or allowed to drive general reading.  The
complete local regression, including the isolated branch tests, passes
258/258.

## Preserved completed result

The v1.43 link-before-pool anchor trajectory development attempt remains
permanently rejected: forward, representation reverse, and every frozen
control all produced zero cross-source supports.  Its attempt marker must not
be removed and the run must not be repeated.

The immediately proposed ordinal-flux v1 branch was then rejected before any
implementation or data access.  Exhaustive analysis showed that its three
pair currents formed a disguised low-cardinality global count.  Its no-go
charter remains a design record only.

## New isolated development candidate

The only retained new family is local ordinal-moment v2:

1. Split each channel of one complete 100 ms window into eight equal,
   mirror-closed microbins.
2. Compute widened doubled medians `M[0..7]` and local roughness magnitudes
   `e[j] = abs(M[j+1]-M[j])`.
3. Convert the seven magnitudes to tie-safe scores
   `r[j] = #lower - #higher`.
4. With `x[j]=j-3`, retain only
   `C=sum((x[j]^2-4)r[j])` and `F=sum(x[j]r[j])`.
5. Use the exact key `(C,F)`; raw reversal is exactly `(C,F)->(C,-F)`.

This is neither an old anchor/count feature nor a complete trajectory.  `F=0`
occurrences remain reportable but cannot provide oriented support.

The frozen files and SHA-256 values are:

- charter `A_ANN_LOCAL_ORDINAL_MOMENT_BRANCH_CHARTER_V2.md`:
  `58C94D46732C5A2751A6B6797EAC17C446EE18AF3DC7BE9041D3F911F3B6F095`;
- implementation `ann_local_ordinal_moment_v2_0.py`:
  `F08C1CB633941F5C3640638464CD4C7BD352E1D42382E4845C6A3746E38A9857`;
- tests `test_ann_local_ordinal_moment_v2_0.py`:
  `0EFB05A7CB50E7498477A4993C7AFA1353FFB1319689E9DFFCFD5F46A6A5E580`.

Changing any of these files invalidates this handoff and requires a new
pre-data review; it does not authorize rerunning on already opened materials.

## Completed synthetic gate

The candidate's synthetic-only phase is complete:

- all 5,040 strict ranks give 1,735 keys, 1,692 oriented keys, median preimage
  3, and maximum preimage 13;
- all 47,293 weak orders satisfy exact reversal, including every tie pattern;
- raw PCM reversal, double reversal, polarity, affine safe-integer fixtures,
  channel permutation/duplication, and within-microbin permutations pass;
- exactly 28 adjacency and 31 linkage controls are internally constructed;
- the sole decision entry point accepts only raw 3+3 segments, canonicalizes
  occurrence order, derives reverse/evidence/invariants/capacities internally,
  and returns only retain or reject;
- rebinding the frozen manifest is rejected;
- duplicated exact evidence and one-key support are rejected;
- targeted tests pass 19/19 and the full suite passes 258/258.

Independent mathematical, fail-open red-team, and test/hash audits approved
closure of the synthetic phase.  None of those audits opened an archive or
media file.

The constructed positive fixture used affine copies of one seeded synthetic
segment.  Its 123 forward versus 14 reverse and maximum-control 18 result proves
only algebra/control plumbing.  It is not evidence of independent-source
transfer and must never be reported as such.

## New-material state: four of nine, all unopened

The user supplied four new paths.  They were role-frozen solely from the paths
and recording timestamps present in the user message; no filesystem operation
was performed on any video:

- F1: `螢幕錄製 2026-08-29 030156.mp4`;
- F2: `螢幕錄製 2026-08-29 030437.mp4`;
- F3: `螢幕錄製 2026-08-29 030634.mp4`;
- H1: `螢幕錄製 2026-08-29 032547.mp4`.

Their independence, duration, hashes, audio layout, and compatibility remain
unverified because they are unopened.  Their frozen absolute paths are in
`A_ANN_LOCAL_ORDINAL_MOMENT_NEW_MATERIAL_ROLE_MANIFEST_V2_20260829.md`.

Still required, in acquisition order:

- H2 and H3: two more wholly new held-out development recordings;
- X1, X2, and X3: three more wholly new sealed external recordings.

Each should contain at least 6.4 seconds of usable audio; at least 8 seconds
with the same capture setup is recommended.  Do not supply copies, transcodes,
excerpts, or splits of one longer recording as independent sources.

## What to do when the remaining five paths arrive

Before opening any source:

1. Register the five paths as H2, H3, X1, X2, X3 by acquisition order.
2. Finalize the role manifest; compute and externally record its SHA-256.
3. Compute all nine raw container hashes.  All must be distinct.
4. Build and freeze the real-data runner that binds 100 ms to decoded sample
   rate, uses only the first 64 complete windows, and reports capacity,
   distinct-key, `F=0`, 2-of-3, and 3-of-3 incidence.
5. Write an immutable single-shot attempt marker containing the source roles
   and hashes, manifest/charter/code/test/runner hashes, geometry, controls,
   and unopened state.

Only then decode F1–F3 and H1–H3 once to finite channel-preserving neutral
`float32` PCM.  Keep X1–X3 sealed.  If equal-capacity inputs cannot be built,
record Inconclusive and stop without replacing a source.  Otherwise execute
exactly one development evaluation.

Development Pass requires all of the following:

- at least two oriented dictionary keys recur in at least two formation and
  two held-out sources;
- forward support strictly exceeds aggregate raw reverse and every one of the
  59 individual controls;
- each held-out source has strictly more forward than reverse dictionary keys;
- evidence, capacity, hashes, roles, raw reversal, and PCM invariants pass.

Every unfavorable score, equality, evidence collision, or failed mandatory
identity is Fail, not Inconclusive.  A development Fail leaves X1–X3 sealed and
ends this branch.  A Pass permits one separately frozen external-validation
run; it does not automatically activate the reader.

## Binding prohibitions

- Do not open, hash, probe, preview, decode, or feature-read the four registered
  videos until all nine roles are present and the pre-decode procedure begins.
- Do not reuse the old eleven, the 2026-08-28 231157/231316/231543 group, the
  2026-08-28 233248/233446 pair, or any derivative.
- Do not change the provisional reader or activate v2.
- Do not modify the v2 microbins, windows, statistics, rank rule, moments,
  thresholds, controls, source split, or decision inequalities after any new
  source is opened.
- Do not expose X1–X3 during development and do not run them after a development
  Fail or Inconclusive result.
- Do not resume v1.43, ordinal-flux v1, nearby anchor/count variants, CRC,
  persistence, coordinator, semantic, causal, or audiovisual side branches as
  substitute work.

## Current stopping statement

> The genuinely new local ordinal-moment representation has passed its
> synthetic algebra, control, fail-closed, and full-regression gates.  The
> active provisional reader remains unchanged.  F1–F3 and H1 are role-frozen
> but unopened; H2, H3, and sealed X1–X3 are still missing.  The line is now at
> the material-completeness boundary, 4/9, not at an algorithmic blocker.

## Primary continuation files

- `A_ANN_LOCAL_ORDINAL_MOMENT_BRANCH_CHARTER_V2.md`
- `A_ANN_LOCAL_ORDINAL_MOMENT_SYNTHETIC_VALIDATION_20260829.md`
- `A_ANN_LOCAL_ORDINAL_MOMENT_NEW_MATERIAL_ROLE_MANIFEST_V2_20260829.md`
- `A_ANN_LOCAL_ORDINAL_MOMENT_NEW_MATERIAL_ROLE_MANIFEST_TEMPLATE_V2.md`
- `ann_local_ordinal_moment_v2_0.py`
- `test_ann_local_ordinal_moment_v2_0.py`

